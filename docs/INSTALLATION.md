# Installation Guide

## Developer install (Chrome/Brave)
1. Open `chrome://extensions` (or `brave://extensions`).
2. Enable **Developer mode**.
3. Click **Load unpacked**.
4. Select the `extension/` folder.

## Release install
If you publish to the Chrome Web Store, install from the store.
